on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

name: PR Validation

permissions:
  contents: read

jobs:
  # Call common build workflow
  call-common-build:
    uses: ./.github/workflows/common-build.yml
    secrets: inherit
